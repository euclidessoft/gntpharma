<form method="post" action="{{ path('resultat_evaluation_delete', {'id': resultat_evaluation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat_evaluation.id) }}">
    <button class="btn">Delete</button>
</form>
