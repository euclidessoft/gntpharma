<!DOCTYPE html>

<title>ResultatEvaluation</title>

{% block body %}
    <h1>ResultatEvaluation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resultat_evaluation.id }}</td>
            </tr>
            <tr>
                <th>ScoreCritere</th>
                <td>{{ resultat_evaluation.scoreCritere }}</td>
            </tr>
            <tr>
                <th>CommentaireCritere</th>
                <td>{{ resultat_evaluation.commentaireCritere }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resultat_evaluation_index') }}">back to list</a>

    <a href="{{ path('resultat_evaluation_edit', {'id': resultat_evaluation.id}) }}">edit</a>

    {{ include('resultat_evaluation/_delete_form.html.twig') }}
{% endblock %}
