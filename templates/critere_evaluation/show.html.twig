<!DOCTYPE html>

<title>CritereEvaluation</title>

{% block body %}
    <h1>CritereEvaluation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ critere_evaluation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ critere_evaluation.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ critere_evaluation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('critere_evaluation_index') }}">back to list</a>

    <a href="{{ path('critere_evaluation_edit', {'id': critere_evaluation.id}) }}">edit</a>

    {{ include('critere_evaluation/_delete_form.html.twig') }}
{% endblock %}
