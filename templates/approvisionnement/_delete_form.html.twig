<form method="post" action="{{ path('approvisionnement_delete', {'id': approvisionnement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ approvisionnement.id) }}">
    <button class="btn">
        {{ 'Supprimer'|trans }}
    </button>
</form>
