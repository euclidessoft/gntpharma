<!DOCTYPE html>

<title>TypeSanction</title>

{% block body %}
    <h1>TypeSanction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_sanction.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_sanction.nom }}</td>
            </tr>
            <tr>
                <th>Descritpion</th>
                <td>{{ type_sanction.descritpion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_sanction_index') }}">back to list</a>

    <a href="{{ path('type_sanction_edit', {'id': type_sanction.id}) }}">edit</a>

    {{ include('type_sanction/_delete_form.html.twig') }}
{% endblock %}
