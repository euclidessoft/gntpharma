<form method="post" action="{{ path('reclamation_delete', {'id': reclamation.id}) }}" onsubmit="return confirm('{{'Êtes-vous sûr de vouloir supprimer cet élément'|trans}}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <button class="btn">{{ 'Supprimer'|trans }}</button>
</form>
