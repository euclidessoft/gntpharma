<!DOCTYPE html>

<title>{{ 'Edit Reclamation'|trans }}</title>

{% block body %}
    <h1>{{ 'Editer Reclamation'|trans }}</h1>

    {{ include('reclamation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('reclamation_index') }}">{{ 'back to list'|trans }}</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
