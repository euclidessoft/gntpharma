{% extends 'admin.html.twig' %}
{% block title %} {{ "Commande"|trans }} - GNT Pharma {% endblock %}
{% block finance %}active{% endblock %}
{% block compte %}active{% endblock %}
{% block all_classes %}active{% endblock %}
{% block body %}
<div class="container mt-4 fond-7">
    <h3 class="mt-4 mb-5 text-bold">
        <i class="fa fa-money-bill mr-2"></i>
        {{ "Listes des classes comptables"|trans }}
    </h3>

   <a href="{{ path('compte_new') }}" class="btn btn-success mb-4"><i class="fas fa-plus"> </i>Nouvelle classe</a>
    <table class="table table-success-light table-bordered table-hover table-striped w-100" id="liste">
        <thead>
            <tr>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte in comptes %}
            <tr>
                <td data-label="Numero">{{ compte.numero }}</td>
                <td data-label="actions">
                    <!-- <a title="{{'Voir'|trans}}" id="{{compte.id}}" onclick="load({{ compte.id }})" href="{{ path('compte_show', {'id': compte.id}) }}" class="btn btn-success"><i class="fas fa-eye"></i></a> -->
                    <!-- <a title="{{'Modifier'|trans}}" href="{{ path('compte_edit', {'id': compte.id}) }}" class="btn btn-success-light"><i class="fas fa-edit"></i></a> -->
                    {{ include('compte/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
