<!DOCTYPE html>

<title>New TypeDecision</title>

{% block body %}
    <h1>Create new TypeDecision</h1>

    {{ include('type_decision/_form.html.twig') }}

    <a href="{{ path('type_decision_index') }}">back to list</a>
{% endblock %}
