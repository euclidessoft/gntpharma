<!DOCTYPE html>

<title>Edit TypeDecision</title>

{% block body %}
    <h1>Edit TypeDecision</h1>

    {{ include('type_decision/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('type_decision_index') }}">back to list</a>

    {{ include('type_decision/_delete_form.html.twig') }}
{% endblock %}
