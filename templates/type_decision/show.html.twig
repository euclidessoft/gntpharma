<!DOCTYPE html>

<title>TypeDecision</title>

{% block body %}
    <h1>TypeDecision</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_decision.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_decision.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_decision_index') }}">back to list</a>

    <a href="{{ path('type_decision_edit', {'id': type_decision.id}) }}">edit</a>

    {{ include('type_decision/_delete_form.html.twig') }}
{% endblock %}
