{% extends 'admin.html.twig' %}

{% block body %}
<div class="col-lg-6 mt-4 pb-4 fond-7">
    <h3 class="mt-4 mb-5 text-bold">
        <i class="fa-brands fa-product-hunt mr-2"></i>
        {{ "Détails financement"|trans }}
    </h3>

    <table class="table table-success-light table-bordered text-center table-hover table-striped w-100">
        <tbody>
            <tr>
                <th>Provenance</th>
                <td>{{ financement.provenance }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ financement.motif }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ financement.montant }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ financement.date ? financement.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ financement.Type }}</td>
            </tr>
            <tr>
                <th>Banque</th>
                <td>{{ financement.banque }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ financement.numero }}</td>
            </tr>
        </tbody>
    </table>

    <div class="col-md-4 offset-md-3 mt-4 py-3 fond-7">
        <div class="centrer">
{#            <a class="btn btn-success mr-3" href="{{ path('financement_edit', {'id': financement.id}) }}">#}
{#                <i class="fa fa-edit"></i> Modifier</a>#}
            <a id="{{ financement.id }}" onclick="load({{ financement.id }})" class="btn btn-success-light text-green" href="{{ path('financement_index') }}">
                <i class="fa fa-undo"></i>
                {{ 'Retour'|trans }}
            </a>
        </div>
    </div>

</div>
{% endblock %}
