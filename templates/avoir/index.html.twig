<!DOCTYPE html>

<title>Avoir index</title>

{% block body %}
    <h1>Avoir index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Montant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avoir in avoirs %}
            <tr>
                <td>{{ avoir.id }}</td>
                <td>{{ avoir.date ? avoir.date|date('Y-m-d') : '' }}</td>
                <td>{{ avoir.Montant }}</td>
                <td>
                    <a href="{{ path('avoir_show', {'id': avoir.id}) }}">show</a>
                    <a href="{{ path('avoir_edit', {'id': avoir.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('avoir_new') }}">Create new</a>
{% endblock %}
