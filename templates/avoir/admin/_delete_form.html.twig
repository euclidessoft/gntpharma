<form method="post" action="{{ path('avoir_delete', {'id': avoir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avoir.id) }}">
    <button class="btn">
        {{ 'Supprimer'|trans }}
    </button>
</form>
