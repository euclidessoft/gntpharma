<form method="post" action="{{ path('avoir_delete', {'id': avoir.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avoir.id) }}">
    <button class="btn">
        {{ 'Supprimer'|trans }}
    </button>
</form>
