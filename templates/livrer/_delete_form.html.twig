<form method="post" action="{{ path('livrer_delete', {'id': livrer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livrer.id) }}">
    <button class="btn">
        {{ 'Supprimer'|trans }}
    </button>
</form>
