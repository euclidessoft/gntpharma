<!DOCTYPE html>

<title>{{ 'Réponse'|trans }}</title>

{% block body %}
    <h1>{{ 'Réponse'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Message'|trans }}</th>
                <td>{{ reponse.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reponse_index') }}">back to list</a>

    <a href="{{ path('reponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
