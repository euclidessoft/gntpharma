<form method="post" action="{{ path('reponse_delete', {'id': reponse.id}) }}" onsubmit="return confirm('{{'Êtes-vous sûr de vouloir supprimer cet élément?'|trans}}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse.id) }}">
    <button class="btn">{{ 'Supprimer'|trans }}</button>
</form>
