<form method="post" action="{{ path('type_conge_delete', {'id': type_conge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_conge.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
