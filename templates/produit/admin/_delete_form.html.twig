<form method="post" title="{{ 'Supprimer'|trans }}" action="{{ path('produit_delete', {'id': produit.id}) }}" onsubmit="return confirm('{{ 'Etes vous sûr de vouloir supprimer?'|trans }}');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
