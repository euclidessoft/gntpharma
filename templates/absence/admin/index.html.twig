{% extends 'admin.html.twig' %}
{% block title %} {{ "Lignes de depenses"|trans }} - GNT Pharma {% endblock %}
{% block employe %}active{% endblock %}
{% block absence_index  %}active{% endblock %}
{% block body %}
<div class="col-lg-10 mt-4 fond-7">
    <h3 class="mt-4 mb-5 text-bold">
        <i class="fa fa-money-bill mr-2"></i>
        {{ "Liste des absences"|trans }}
    </h3>
    <a href="{{ path('absence_new') }}" class="btn btn-success mb-4"><i class="fas fa-plus"></i>Nouvel absence</a>
    <table class="table table-success-light table-bordered table-hover table-striped w-100" id="liste">
        <thead>
            <tr>
                <th>{{('Employé'|trans)}}</th>
                <th>{{('Date absence'|trans)}}</th>
                <th>{{('Justifier')}}</th>
                <th>{{('Status')}}</th>
                <th>{{('actions'|trans)}}</th>
            </tr>
        </thead>
        <tbody>
            {% for absence in absences %}
            <tr>
                <td data-label="Nom">{{ absence.employe.nom }}</td>
                <td data-label="Date absence">{{ absence.dateAbsence ? absence.dateAbsence|date('d/m/Y') }}</td>
                <td data-label="Justifier">
                    {% if absence.justifier == false %}
                    <span class="badge badge-danger">Non justifié</span>
                    {% else %}
                    <span class="badge badge-success">Justifié</span>
                    {% endif %}
                </td>
                <td data-label="Status">
                {% if absence.status == 0 %}
                    <span class="badge badge-danger">En attente de justification</span>
                    {% elseif absence.status == 1 %}
                    <span class="badge badge-primary">En cours de traitement</span>
                    {% else %}
                    <span>Justifié</span>
                    {% endif %}
                </td>
                <td data-label="Actions">
                    <a href="{{ path('absence_show', {'id': absence.id}) }}" class="btn btn-success"><i class="fas fa-eye"></i></a>
                    <a href="{{ path('absence_edit', {'id': absence.id}) }}" class="btn btn-success-light"><i class="fas fa-edit"></i></a>
                    {{ include('absence/admin/_delete_form.html.twig') }}
                </td>
            </tr>
            {% else %}
            <tr>
                <td colspan="4" class="text-center">Aucune ligne de dépenses</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    {% endblock %}