<!DOCTYPE html>

<title>PrimePerformance</title>

{% block body %}
    <h1>PrimePerformance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prime_performance.id }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ prime_performance.createAt ? prime_performance.createAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ prime_performance.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prime_performance_index') }}">back to list</a>

    <a href="{{ path('prime_performance_edit', {'id': prime_performance.id}) }}">edit</a>

    {{ include('prime_performance/_delete_form.html.twig') }}
{% endblock %}
