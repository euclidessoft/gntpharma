<form method="post" action="{{ path('prime_performance_delete', {'id': prime_performance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prime_performance.id) }}">
    <button class="btn">Delete</button>
</form>
