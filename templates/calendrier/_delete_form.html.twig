<form method="post" action="{{ path('calendrier_delete', {'id': calendrier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendrier.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash"></i></button>
</form>
